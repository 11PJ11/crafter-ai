meta:
  generated_at: "2026-02-05T18:30:00Z"
  generated_by: "vera-orchestrator"
  project: "modern-cli-installer"
  phase: "10"
  phase_name: "Full Installation Journey (Walking Skeleton Integration)"
  schema_version: "0.2"

step:
  id: "10-07"
  name: "Update celebration to use nWave brand and add Getting started section"
  agent: "software-crafter"
  estimated_hours: 1.5
  dependencies_satisfied: true  # 10-06 completed

input:
  source_file: "src/crafter_ai/installer/cli/forge_install.py"
  test_files:
    - "tests/acceptance/forge_tui/test_walking_skeleton.py::TestWalkingSkeletonBuildToInstall::test_celebration_uses_nwave_brand"
    - "tests/acceptance/forge_tui/test_walking_skeleton.py::TestWalkingSkeletonBuildToInstall::test_getting_started_section"

  requirements:
    celebration_section:
      - "In install() function after health check (currently around line 519)"
      - "Change from: 🎉 crafter-ai {version} installed and healthy!"
      - "Change to: 🎉 nWave {version} installed and healthy!"
      - "Keep: 'Ready to use in Claude Code.' on next line"

    getting_started_section:
      - "Add after the celebration lines"
      - "Add blank line first"
      - "Add header: console.print('  📖 Getting started')"
      - "List main nWave commands with descriptions (dim style):"
      - "  console.print('[dim]    /nw:discuss  - Requirements gathering and business analysis[/dim]')"
      - "  console.print('[dim]    /nw:design   - Architecture design with visual representation[/dim]')"
      - "  console.print('[dim]    /nw:distill  - Acceptance test creation and business validation[/dim]')"
      - "  console.print('[dim]    /nw:develop  - Outside-In TDD implementation with refactoring[/dim]')"
      - "  console.print('[dim]    /nw:deliver  - Production readiness validation[/dim]')"

  test_requirements:
    - "Test test_celebration_uses_nwave_brand must pass"
    - "Test expects: '🎉' (party popper emoji) in output"
    - "Test expects: 'nWave 0.2.0 installed and healthy!' in output"
    - "Test expects: 'Ready to use in Claude Code.' in output"
    - "Test test_getting_started_section must pass"
    - "Test expects: 'Getting started' in output"
    - "Test expects: '/nw:discuss' in output"

  architecture_notes:
    - "Celebration section goes AFTER health check and BEFORE failure check"
    - "Use 'nWave' as the product brand name (not 'crafter-ai' package name)"
    - "Getting started shows the 5 main workflow commands (DISCUSS → DESIGN → DISTILL → DEVELOP → DELIVER)"
    - "All command lines use [dim] style for visual hierarchy"
    - "Command descriptions are short (one line each)"

  luna_design_reference:
    - "docs/ux/modern-cli-installer/journey-forge-tui-visual.md lines 192-200"
    - "Celebration format: emoji + 'nWave {version} installed and healthy!'"
    - "Getting started: 📖 header + 5 command lines in dim"
    - "Note: 🆕 What's new section is optional (for upgrades with changelog)"

constraints:
  max_tool_calls: 15
  must_run_tests: true
  fail_fast: true
  forbidden_actions:
    - "DO NOT skip running E2E test first"
    - "DO NOT modify scripts/testpypi_validation.py"
    - "DO NOT read roadmap.yaml or execution-log.yaml"

output:
  status: "success"
  artifacts_created:
    - "src/crafter_ai/installer/cli/forge_install.py"
  test_command: "pytest tests/acceptance/forge_tui/test_walking_skeleton.py::TestWalkingSkeletonBuildToInstall::test_celebration_uses_nwave_brand tests/acceptance/forge_tui/test_walking_skeleton.py::TestWalkingSkeletonBuildToInstall::test_getting_started_section -v"
  test_result: "2 passed in 0.35s"
  commit_hash: "b0979c2"
  commit_message: "feat(forge-install): update celebration to use nWave brand and add Getting started section"
  notes: "Changed celebration from 'crafter-ai' to 'nWave' product brand. Added Getting started section with 5 main nWave commands (discuss, design, distill, develop, deliver). All command lines use [dim] style for visual hierarchy."
