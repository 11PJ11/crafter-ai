[cosmic-ray]
module-path = "scripts/install/plugins"
timeout = 30.0
excluded-modules = ["__init__"]
test-command = "pytest tests/nwave/plugin-architecture/ -x -q --tb=no"

[cosmic-ray.distributor]
name = "local"

[cosmic-ray.execution-engine]
name = "local"
