[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pyyaml = ">=6.0"
packaging = ">=21.0"

[dev-packages]
# Testing
pytest = ">=7.0.0"
pytest-bdd = ">=7.0.0"
pytest-cov = ">=4.0.0"

# Linting and formatting (ruff replaces black + flake8)
ruff = ">=0.3.0"
mypy = ">=0.991"

# CLI and utilities
click = ">=8.0.0"
colorama = ">=0.4.0"
tqdm = ">=4.64.0"

# Pre-commit hooks
pre-commit = ">=3.0.0"

[scripts]
# Core CI/CD commands
test = "python -m pytest tests/ -v"
build = "python tools/build.py"

# Linting and formatting
lint = "ruff check scripts/ tools/ tests/"
format = "ruff format scripts/ tools/ tests/"
format-check = "ruff format --check scripts/ tools/ tests/"

# Validation
validate = "python scripts/local_ci.py"
validate-fast = "python scripts/local_ci.py --fast"
validate-yaml = "python scripts/validation/validate_yaml_files.py"

[requires]
python_version = "3"
