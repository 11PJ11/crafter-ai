[pytest]
# Add src directory to Python path for module discovery
pythonpath = src .
testpaths = tests
markers =
    e2e: End-to-end acceptance tests
    integration: Integration tests for component boundary collaborations
    unit: Unit tests for individual domain objects
    slow: Tests that take a long time to run
