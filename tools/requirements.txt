# 5D-WAVE IDE Bundle Builder Dependencies

# Core dependencies
PyYAML>=6.0
pathlib>=1.0.1

# Optional dependencies for enhanced functionality
click>=8.0.0  # For better CLI interface (if needed)
colorama>=0.4.0  # For colored console output (if needed)
tqdm>=4.64.0  # For progress bars (if needed)

# Development dependencies (for testing and validation)
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0
mypy>=0.991